{
  "name": "personal-page",
  "version": "1.0.0",
  "private": true,
  "description": "Personal site — static HTML with a compiled Tailwind stylesheet.",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
